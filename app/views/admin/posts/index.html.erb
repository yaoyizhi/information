<table class="table table-boldered">
    <tbody>
        <% @posts.each do |post| %>
            <tr>
                <td><%=link_to(post.title, admin_post_path(post))%></td>
                <td><%= post.created_at %></td>
            </tr>
        <% end %>
    </tbody>
</table>
